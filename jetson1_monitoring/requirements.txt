# Python dependencies for JETSON1_INTEGRATED.py
# Jetson Orin Nano용

# MQTT 통신
paho-mqtt>=1.6.1

# 이미지 처리
Pillow>=9.0.0

# YOLO 객체 감지
ultralytics>=8.0.0

# NumPy (대부분 이미 설치되어 있음)
numpy>=1.21.0

# OpenCV는 Jetson에 기본 설치되어 있으므로 여기서 제외
# opencv-python>=4.5.0  # Jetson에서는 시스템 패키지 사용

# GStreamer Python 바인딩 (시스템 패키지로 설치 필요)
# PyGObject는 apt로 설치: sudo apt install python3-gi
